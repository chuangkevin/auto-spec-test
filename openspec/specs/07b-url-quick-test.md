# 功能模組：URL 快速測試（智慧探索模式）

## 設計概念

使用者只需貼上一個網址，系統透過內嵌瀏覽器載入頁面，AI 自動掃描並辨識頁面上的可測試元件，規劃測試流程，產出任務清單。使用者確認後即可開始逐條視覺化測試。

**核心流程：**

```text
貼上網址 → 內嵌瀏覽器載入 → AI 掃描元件 → 產出任務清單 → 使用者調整 → 逐條執行 → 報告
```

## 頁面流程

### Phase 1：輸入網址 & 掃描

```text
┌─────────────────────────────────────────────────────────────┐
│  [🔗 請輸入目標網址 ________________________] [開始掃描]      │
│  [測試帳號（選填）______] [測試密碼（選填）______]              │
│  [📎 附加規格書（選填）：拖拉上傳 .md .docx .xlsx .csv]       │
├────────────────────────────────┬────────────────────────────┤
│                                │                            │
│     內嵌瀏覽器畫面              │  🔍 AI 掃描中...            │
│                                │                            │
│     ┌────────────────────┐    │  發現的元件：               │
│     │                    │    │  ┌─ 登入表單 ─────────┐    │
│     │   [AI 正在掃描頁面]  │    │  │ 帳號輸入框 (input)  │    │
│     │   [元件高亮標記中]   │    │  │ 密碼輸入框 (input)  │    │
│     │                    │    │  │ 登入按鈕 (button)   │    │
│     └────────────────────┘    │  └────────────────────┘    │
│                                │  ┌─ 導覽列 ──────────┐    │
│                                │  │ 首頁 (link)        │    │
│                                │  │ 關於我們 (link)     │    │
│                                │  └────────────────────┘    │
│                                │                            │
└────────────────────────────────┴────────────────────────────┘
```

### Phase 2：任務清單確認

```text
┌─────────────────────────────────────────────────────────────┐
│  AI 已規劃 12 個測試案例      [+ 新增案例] [▶ 開始測試]       │
├────────────────────────────────┬────────────────────────────┤
│                                │  測試任務清單               │
│     內嵌瀏覽器畫面              │                            │
│     (供使用者參照)              │  ☑ TC-001 正確帳密登入  ≡  │
│                                │  ☑ TC-002 空白帳號送出  ≡  │
│                                │  ☑ TC-003 錯誤密碼提示  ≡  │
│                                │  ☐ TC-004 SQL 注入測試  ≡  │
│                                │  ☑ TC-005 記住我功能    ≡  │
│                                │  ☑ TC-006 忘記密碼連結  ≡  │
│                                │                            │
│                                │  ≡ = 拖曳排序手柄           │
│                                │  ☑/☐ = 勾選是否執行         │
│                                │                            │
│                                │  點擊案例可展開查看/編輯：   │
│                                │  ┌─ TC-001 正確帳密登入 ──┐│
│                                │  │ 步驟：                  ││
│                                │  │ 1. 輸入帳號 admin       ││
│                                │  │ 2. 輸入密碼 ****        ││
│                                │  │ 3. 點擊登入按鈕         ││
│                                │  │ 預期：跳轉至首頁        ││
│                                │  │ [編輯] [刪除]           ││
│                                │  └────────────────────────┘│
└────────────────────────────────┴────────────────────────────┘
```

### Phase 3：執行測試

與 `07-test-execution.md` 的測試執行流程相同，包含即時視覺化、燈號狀態、手動介入等完整功能。

### Phase 4：測試完成

```text
┌─────────────────────────────────────────────────────────────┐
│  測試完成！通過 8 / 失敗 2 / 跳過 2                           │
├────────────────────────────────┬────────────────────────────┤
│                                │  測試結果                   │
│     最後一張截圖                │                            │
│     或測試摘要圖表              │  🟢 TC-001 正確帳密登入     │
│                                │  🟢 TC-002 空白帳號送出     │
│                                │  🔴 TC-003 錯誤密碼提示     │
│                                │  🟢 TC-005 記住我功能       │
│                                │  🔴 TC-006 忘記密碼連結     │
│                                │  ...                       │
│                                │                            │
├────────────────────────────────┴────────────────────────────┤
│  [📄 檢視完整報告] [⬇️ 下載報告] [💾 存為專案] [🔄 重新測試]  │
└─────────────────────────────────────────────────────────────┘
```

## AI 元件掃描流程

1. Playwright 載入目標網址
2. 等待頁面載入完成（networkidle）
3. 擷取頁面截圖 + 完整 DOM 結構
4. 送至 Gemini 2.5 Flash 進行分析：
   - 辨識頁面上所有可互動元件（input、button、select、link、form 等）
   - 分類元件功能（登入表單、搜尋列、導覽選單、資料表格等）
   - 若有附加規格書，對照規格補充測試案例
5. AI 根據發現的元件，規劃測試案例：
   - 正向測試（正常操作流程）
   - 負向測試（空白輸入、錯誤資料）
   - 邊界測試（極長文字、特殊字元）
   - UI 測試（元件是否可見、對齊、響應式）
6. 回傳結構化的任務清單

## 掃描結果與規格書的結合

| 情境 | 行為 |
|------|------|
| 無規格書 | AI 純粹根據頁面元件規劃測試（智慧探索） |
| 有規格書 | AI 先解析規格書，再對照頁面元件，產出更完整的測試案例 |
| 規格書有但頁面缺少對應元件 | 標記為「規格要求但頁面未實作」，列為 Bug 候選 |
| 頁面有但規格書未提及的元件 | 仍產出基本測試案例，標記為「額外發現」 |

## 附加規格書上傳

| 項目 | 規格 |
|------|------|
| 支援格式 | `.md` `.docx` `.xls` `.xlsx` `.csv` |
| 上傳方式 | 拖拉或點擊選取，支援多檔案 |
| 是否必填 | 否（純 URL 探索模式不需要） |
| 處理流程 | 與規格書解析流程相同，解析後作為 AI 掃描的額外上下文 |

## 存為專案

URL 快速測試完成後，使用者可選擇「存為專案」：

1. 輸入專案名稱、選擇產品
2. 系統自動將 AI 產出的任務清單轉換為標準測試腳本格式
3. 測試報告自動關聯到新專案
4. 後續可在專案中重複執行測試
